# 生成上下文

下面是本次生成的必要约束。请严格遵守，但不要在页面中解释这些规则。

优先级：用户明确需求 > 选中的设计系统 > 产物类型规则 > 通用设计规则 > 用户表单里的主题色/辅助色/组件风格。


## 通用设计规则（精简）

- 使用清晰的信息层级、真实业务文案和可扫描布局，不要输出模板感占位内容。
- 字体控制在 6-8 个层级内；正文 15-18px，行高 1.5-1.6；按钮/标签文字保持可读。
- 色彩需要有主次、背景、中性色和状态色；避免整页只用单一色系的浅深变化。
- 页面必须响应式，移动端不能文字溢出或元素重叠。
- 组件状态要完整：hover/focus/active/disabled/loading 等按场景补齐。


## 产物类型规则（web-prototype，精简）

- 生成可交互的 Web 原型页面，优先完成用户描述的页面结构、核心流程和关键状态。
- 用真实示例数据呈现列表、表格、卡片、表单和空状态。
- 不做营销式 landing，除非用户明确要求。


# 用户原始需求

你是专业的前端工程师和UI/UX设计师。
请生成一个高保真的HTML 原型页面。

# 技术栈
- Tailwind CSS (CDN)
- Vue 3 (CDN, 可选)
- FontAwesome (CDN)
- ECharts (如需图表)
- Google Fonts (Inter)

# 全局设计规范
- 主色: #004fff
- 强调色: #10b981
- 背景模式: 浅色
- 组件风格: Ant Design
- 设计系统: 默认风格
- 产物类型: web-prototype
- 约束优先级: 用户明确需求 > 设计系统 > 产物类型工作流 > craft 规则 > 主色/强调色/组件风格
- 如果选择了具体设计系统，主色和强调色只作为微调偏好，不要覆盖该设计系统的核心品牌色、字体和组件语言
- 圆角: 0.5rem
- 阴影: 使用柔和现代的阴影


# 页面需求

## 页面1: 页面1
**布局描述**: 完整还原下这个页面，然后在扫码提交规则设置后方，增加一个tab，名称是通用题卡重复规则设置，选中

页面显示：

规则设置：默认，自定义时长，不生效，默认选中“默认”
1. 选中默认，下方显示解释文案：扫描题卡的序号之前已被使用过，且时间间隔超过7天，则会提醒老师需要手动选择题卡归属的作业。
其中天数标记高亮
2.选中自定义时长，下方显示可输入的天数，默认7天，可以调整数字，最少1天，最多可以设置180天。
解释文案：扫描题卡的序号之前已被使用过，且时间间隔超过x天，则会提醒老师需要手动选择题卡归属的作业。    其中x为设置的天数，也需要高亮

3.选中不生效
解释文案：不再进行序号重复性验证，若序号重复则扫描题卡自动归属到最近一份相同序号的作业中。




**参考图**: 已附加1张参考图。请先识别参考图中的页面类型、信息架构、主要模块、文字线索、视觉层级和交互控件；参考图是本页生成的核心依据，不要忽略，也不要自行替换成无关业务页面。参考图模式：像素级还原。必须严格还原参考图的布局、比例、颜色、字体层级、组件样式、内容密度、图标/图片位置和可读文字；不得主动改动参考图中的主要结构、视觉风格、模块顺序和关键文案。用户文字需求只用于补充缺失信息，不要改成无关主题。

# 输出要求（重要！）

请输出一个**完整的、独立的HTML文件**。

要求：
1. 所有CSS放在<style>标签中
2. 所有JS放在<script>标签中
3. 使用真实的示例数据（不要Lorem ipsum）
4. 响应式设计
5. 直接可在浏览器中打开使用

输出格式：
```html
<!DOCTYPE html>
<html lang="zh-CN">
...完整代码...
</html>
```


# 图片资源路径与用途
所有图片已保存到项目文件夹的 `reference/` 目录。生成 HTML 时请使用这些相对路径作为 `<img src="reference/...">`。
非 PPT 参考图是页面生成的重要依据，但必须严格按每页的参考图模式执行：仅参考布局、仅参考风格、像素级还原三者互斥。请先识别参考图，再按指定模式取舍；不要忽略参考图，也不要替换成无关业务页面。
页面 1 的参考图模式：像素级还原。必须严格还原参考图的页面类型、布局、比例、颜色、字体层级、组件样式、内容密度、图标/图片位置和可读文字；不得主动改动参考图中的主要结构、视觉风格、模块顺序和关键文案。用户文字需求只用于补充缺失信息，不要改成无关主题。
- 参考图: reference/ref_1.png


# 最终输出约束
- 输出一个完整、独立、可直接预览的 HTML 文件。
- 如果产物类型要求 deck/PPT，也输出单文件 HTML deck，保存为 index.html。
- 不要输出 Markdown 解释，不要输出多个文件名说明，只返回 HTML。